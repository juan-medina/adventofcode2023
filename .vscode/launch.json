{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/internal/app",
            "args": ["-day", "3", "-part", "1", "-test"],
            "cwd": "${workspaceFolder}"
        }
    ]
}